# Json

## How to install

1. Add the package ```philipschlender/json-php``` to the ```require``` or ```require-dev``` element of the ```composer.json``` file

    ```bash
    composer require philipschlender/json-php
    ```
